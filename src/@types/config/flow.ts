export interface FlowConfig {
  maxColumns: number;
  minCharsPerColumn: number;
  maxCharsPerColumn: number;
  columnGap: number;

  fontSize: number;
  lineHeight: number;
  textAlign: number | null;
}
